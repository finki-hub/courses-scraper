name: CAS Smoke Test

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cas-smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cas-smoke:
    name: CAS Smoke Test
    if: github.ref == 'refs/heads/main'
    uses: finki-hub/.github/.github/workflows/pytest.yaml@d10f74eb5f2d9b377627dd390c4081c8b10867fc
    with:
      pytest-args: "-q tests/test_cas_live.py"
      require-cas-credentials: true
    secrets:
      CAS_USERNAME: ${{ secrets.CAS_USERNAME }}
      CAS_PASSWORD: ${{ secrets.CAS_PASSWORD }}
