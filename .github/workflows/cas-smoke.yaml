name: CAS Smoke Test

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cas-smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cas-smoke:
    name: CAS Smoke Test
    if: github.ref == 'refs/heads/main'
    uses: finki-hub/.github/.github/workflows/pytest.yaml@430a2e5c15088723231f6743cb3b6e1fc7411349
    with:
      pytest-args: "-q tests/test_cas_live.py"
      require-cas-credentials: true
    secrets:
      CAS_USERNAME: ${{ secrets.CAS_USERNAME }}
      CAS_PASSWORD: ${{ secrets.CAS_PASSWORD }}
